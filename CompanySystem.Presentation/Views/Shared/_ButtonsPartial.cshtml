@model string

<td>
    <a asp-action="Details" asp-route-id="@Model" class="btn btn-warning">
        <i class="fas fa-eye"></i> Details
    </a>
</td>
<td>
    <a asp-action="Edit" asp-route-id="@Model" class="btn btn-success">
        <i class="fas fa-edit"></i> Update
    </a>
</td>

<td>
    @if (ViewContext.RouteData.Values["controller"]?.ToString() == "Employee")
    {
        <!-- Delete Button trigger modal -->
        <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal-@Model">
            <i class="fas fa-trash"></i>Delete
        </button>
    }
    else
    {
        <!-- Delete View for Department and Users Roles view--> 
        <a asp-action="Delete" asp-route-id="@Model" class="btn btn-danger">
            <i class="fas fa-trash"></i>Delete
        </a>
    }
</td>
